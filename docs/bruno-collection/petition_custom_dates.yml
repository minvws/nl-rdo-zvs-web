info:
  name: petition_custom_dates
  type: http
  seq: 9

http:
  method: GET
  url: "{{host}}/api/v1/petition_custom_dates/"
  headers:
    - name: ""
      value: ""
      disabled: true
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
