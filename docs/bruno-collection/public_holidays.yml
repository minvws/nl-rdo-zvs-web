info:
  name: public_holidays
  type: http
  seq: 23

http:
  method: GET
  url: "{{host}}/api/v1/public_holidays/"
  headers:
    - name: ""
      value: ""
      disabled: true
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
