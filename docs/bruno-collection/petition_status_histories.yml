info:
  name: petition_status_histories
  type: http
  seq: 17

http:
  method: GET
  url: "{{host}}/api/v1/petition_status_histories/"
  headers:
    - name: ""
      value: ""
      disabled: true
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
