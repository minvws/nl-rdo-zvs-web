info:
  name: contacts
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/api/v1/contacts/"
  headers:
    - name: ""
      value: ""
      disabled: true
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
