info:
  name: departments
  type: http
  seq: 25

http:
  method: GET
  url: "{{host}}/api/v1/departments/"
  headers:
    - name: ""
      value: ""
      disabled: true
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
