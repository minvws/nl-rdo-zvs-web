info:
  name: petition_petition
  type: http
  seq: 14

http:
  method: GET
  url: "{{host}}/api/v1/petition_petition/"
  headers:
    - name: ""
      value: ""
      disabled: true
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
