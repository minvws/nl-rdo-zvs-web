info:
  name: users
  type: http
  seq: 26

http:
  method: GET
  url: "{{host}}/api/v1/users/"
  headers:
    - name: ""
      value: ""
      disabled: true
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
