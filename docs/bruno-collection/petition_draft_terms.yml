info:
  name: petition_draft_terms
  type: http
  seq: 12

http:
  method: GET
  url: "{{host}}/api/v1/petition_draft_terms/"
  headers:
    - name: ""
      value: ""
      disabled: true
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
