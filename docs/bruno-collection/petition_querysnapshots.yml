info:
  name: petition_querysnapshots
  type: http
  seq: 16

http:
  method: GET
  url: "{{host}}/api/v1/petition_querysnapshots/"
  headers:
    - name: ""
      value: ""
      disabled: true
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
