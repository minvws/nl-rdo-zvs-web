info:
  name: petition_deliverables
  type: http
  seq: 11

http:
  method: GET
  url: "{{host}}/api/v1/petition_deliverables/"
  headers:
    - name: ""
      value: ""
      disabled: true
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
