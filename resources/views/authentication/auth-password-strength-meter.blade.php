<div>
    <div class="progress-container">
        <div class="progress-bar">
            <div></div>
        </div>
    </div>
    <div
        class="strength-text-container"
        aria-live="polite">
        <span class="strength-text weak">{{ __('authentication.password-strength.strength.weak') }}</span>
        <span class="strength-text moderate">{{ __('authentication.password-strength.strength.moderate') }}</span>
        <span class="strength-text strong">{{ __('authentication.password-strength.strength.strong') }}</span>
        <span class="strength-text very-strong">
            {{ __('authentication.password-strength.strength.very-strong') }}
        </span>
        <span class="help-text">{{ __('authentication.password-strength.help') }}</span>
    </div>
</div>
